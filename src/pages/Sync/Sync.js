import React from 'react';
import './Sync.scss';

const Sync = () => (
  <div className="sync-container">
    <span>:((</span>
    <span>Seems a bit empty in here...</span>
    <button type="button" className="sync-button">Sync</button>
  </div>
);

export default Sync;
